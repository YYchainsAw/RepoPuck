# RepoPuck

A lightweight, tray-first Git client for staging, committing, and pushing from a dockable Windows panel.

> Small on screen. Fast in flow.

Development happens on the `develop` branch. The first release is being built with Tauri, Rust, React, and TypeScript.

